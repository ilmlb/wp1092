import React, { useState }  from "react";
export default ({}) => {
    let intToHeader = (i) => {
        //
    }
    const [header, setHeader] = useState(26);

    return <div class="header">{}</div>
}
