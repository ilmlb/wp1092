import React, { useState }  from "react";
export default ({}) => {
    let intToIndex = (i) => {
        //
    }
    const [index, setIndex] = useState(100);

    return <div class="index">{}</div>
}